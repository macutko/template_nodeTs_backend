{
  "name": "server",
  "version": "0.0.0",
  "description": "Recru streaming server",
  "main": "dist/index.ts",
  "scripts": {
    "build": "tsc",
    "serve": "node dist/index.js",
    "clean": "rm -rf dist/",
    "lint": "eslint ./src --ext .ts --fix",
    "start": "docker-compose up --build --force-recreate --remove-orphans",
    "start:local": "nodemon"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "helmet": "^4.6.0",
    "nodemon": "^2.0.7",
    "typescript": "^4.3.4",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "^15.12.5"
  }
}
